@using FINANCE.TRACKER.ViewModels.Category
@model ExpensesCategoryViewModel;

<div id="expenses-category-table" class="inner-forms">
    <div class="wrapper row">
        <section class="forms">
            <table id="tblExpensesCategory" style="width: 100%;" class="table table-hover" cellspacing="0">
                <thead>
                    <tr>
                        <th>Expenses Category Name</th>
                        <th>Description</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </section>
        <div class="modal fade" id="modalExpensesCategoryDetails" tabindex="-1" role="dialog" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header primary-background">
                        <h5 class="modal-title" id="expensesCategoryTitle">New Expenses Category</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form id="expensesCategoryForm" method="post">
                            <div class="col-md-12 mb-3">
                                <label asp-for="ExpensesCategoryName" class="form-label">Expenses Category Name</label>
                                <input asp-for="ExpensesCategoryName" class="form-control pascal-input" autocomplete="off" />
                                <span asp-validation-for="ExpensesCategoryName" class="text-danger"></span>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label asp-for="ExpensesCategoryDescription" class="form-label">Description</label>
                                <textarea asp-for="ExpensesCategoryDescription" class="form-control" autocomplete="off"></textarea>
                                <span asp-validation-for="ExpensesCategoryDescription" class="text-danger"></span>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label for="chkExoensesCategoryStatus">Status</label><br />
                                <input id="chkExoensesCategoryStatus" type="checkbox" checked data-on-text="Active" data-off-text="Inactive" data-on-color="success" data-off-color="danger">
                            </div>
                            <hr />
                            <div class="d-flex justify-content-end">
                                <button id="btnClearExpensesCategoryForm" type="reset" class="btn btn-secondary me-2">Clear</button>
                                <button id="btnSubmitExpensesCategory" type="button" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Add Expenses Category</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>