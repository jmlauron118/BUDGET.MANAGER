@using FINANCE.TRACKER.ViewModels.UserManager
@model RoleViewModel;

<div id="role-table" class="inner-forms">
    <div class="wrapper row">
        <section class="forms">
            <table id="tblRoles" style="width: 100%;" class="table table-hover" cellspacing="0">
                <thead>
                    <tr>
                        <th>Role</th>
                        <th>Description</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </section>
        <div class="modal fade" id="modalRoleDetails" tabindex="-1" role="dialog" aria-hidden="true">
            <div class="modal-dialog modal-sm" role="document">
                <div class="modal-content">
                    <div class="modal-header primary-background">
                        <h5 class="modal-title" id="roleModalTitle">New Role</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form id="roleForm" method="post">
                            <div class="col-md-12 mb-3">
                                <label asp-for="Role" class="form-label">Role</label>
                                <input asp-for="Role" class="form-control pascal-input" autocomplete="off" />
                                <span asp-validation-for="Role" class="text-danger"></span>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label asp-for="RoleDescription" class="form-label">Description</label>
                                <textarea asp-for="RoleDescription" class="form-control" autocomplete="off"></textarea>
                                <span asp-validation-for="RoleDescription" class="text-danger"></span>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label for="chkRoleStatus">Status</label><br />
                                <input id="chkRoleStatus" type="checkbox" checked data-on-text="Active" data-off-text="Inactive" data-on-color="success" data-off-color="danger">
                            </div>
                            <hr />
                            <div class="d-flex justify-content-end">
                                <button id="btnClearRoleForm" type="reset" class="btn btn-secondary me-2">Clear</button>
                                <button id="btnSubmitRole" type="button" class="btn btn-primary"><i class="fa fa-plus" aria-hidden="true"></i> Add Role</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>